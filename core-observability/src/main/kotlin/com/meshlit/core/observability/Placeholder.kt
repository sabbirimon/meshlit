package com.meshlit.core.observability

/**
 * Placeholder — populated by the tracing work in §6.
 * Exists so the new :core-observability module compiles before
 * the actual TracingController / TracerHolder / OtelBootstrap
 * classes land.
 */
internal object Placeholder
